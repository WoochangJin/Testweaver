# TestWeaver

> FastAPI 프로젝트를 분석해 **무엇을 테스트해야 하는지 먼저 설계**하고,
> 사용자가 고른 시나리오를 실행 가능한 테스트 코드로 짜주는 AI 기반 테스트 설계 도구.

---

## 1. 프로젝트명

**TestWeaver** (테스트위버)

테스트 케이스를 하나하나 엮어(weave) 완성된 테스트 설계표를 짜낸다는 의미를 담았습니다. 개별 테스트 코드를 뽑아주는 기존 도구들과 달리, TestWeaver는 흩어진 기능·제약·예외 흐름을 하나의 짜임새 있는 테스트 설계로 엮어내는 것을 지향합니다.

---

## 2. 개발 목적

### 문제 정의

개발자가 테스트를 잘 못 쓰는 이유는 대개 **코드 작성법을 몰라서가 아닙니다.** 진짜 병목은 그 앞 단계에 있습니다.

- 무엇을 테스트해야 할지 판단하기 어렵다
- 정상 케이스만 확인하고 예외·경계 케이스를 놓친다
- API·의존성·DB·권한 조건이 얽혀 있어 어디서부터 손대야 할지 막막하다
- 테스트에 필요한 mock 데이터를 만드는 일이 번거롭다
- 기존 프로젝트의 테스트 스타일에 맞추기 어렵다

즉 "테스트 코드를 어떻게 쓰는가"보다 **"무엇을, 어떤 경우까지 테스트해야 하는가"를 설계하는 일**이 더 큰 장벽입니다.

### 기존 도구의 한계

현재 나와 있는 테스트 생성 도구들은 대부분 **함수·메서드 단위로 곧바로 테스트 코드를 뱉는 방식**입니다. 이 방식은 두 가지 약점이 있습니다.

첫째, 개발자가 "어떤 함수를 테스트할지" 이미 알고 있다는 전제가 깔려 있어, 정작 어려운 **"무엇을 테스트할지 결정하는 단계"를 건너뜁니다.** 둘째, 테스트해야 할 케이스를 사람이 검토하고 선택할 여지 없이 일괄 생성되기 때문에, 정작 중요한 예외·경계 케이스가 누락되어도 알아차리기 어렵습니다.

특히 **FastAPI 생태계**에는 프로젝트 전체를 분석해 기능과 시나리오를 설계해주는 도구가 사실상 비어 있습니다. 기존 Python 테스트 생성 도구들은 함수 시그니처 기반의 테스트 스켈레톤을 만들어주는 수준에 머물러 있으며, FastAPI 고유의 구조(의존성 주입, 스키마 기반 입력 검증, 예외 처리)를 깊이 이해하지는 못합니다.

### 개발 목적

TestWeaver는 테스트 코드를 바로 생성하기 전에 **테스트 설계표(Test Design Matrix)를 먼저 만들어주는 것**을 목적으로 합니다. 프로젝트 구조와 실행 흐름을 분석해 테스트가 필요한 기능과 누락되기 쉬운 시나리오를 찾아내고, 의존성·입력 제약·예외 흐름을 반영한 실행 가능한 테스트를 생성합니다.

FastAPI를 첫 번째 지원 대상으로 삼은 이유는 이 프레임워크가 테스트 설계에 필요한 정보를 코드 안에 명시적으로 담고 있기 때문입니다. Pydantic 모델에는 입력 제약(타입·필수 여부·범위·형식)이, 의존성 주입(`Depends`) 구조에는 대체·검증 지점이, 예외 처리기에는 실패 흐름이 구조화되어 있어, 정적 분석만으로도 높은 정확도의 기능·케이스 추출이 가능합니다.

---

## 3. 프로젝트 소개

### 전체 흐름

```
프로젝트 분석
    ↓
기능 단위 추출
    ↓
기능별 테스트 케이스 후보 생성
    ↓
사용자가 케이스 선택 / 조합
    ↓
실행 가능한 테스트 코드 생성
    ↓
실행 결과 확인 및 수정 제안
```

### 핵심 기능

**① 테스트 대상 기능 추출**

FastAPI 프로젝트를 정적으로 분석해 테스트가 필요한 단위를 자동으로 식별합니다.

- 라우터(엔드포인트) 및 HTTP 메서드
- Pydantic 모델 기반 입력 제약 조건
- 의존성 주입(`Depends`) 구조 및 대체 지점
- 예외 처리기 및 에러 응답 흐름
- 인증·권한 조건
- 외부 서비스·DB 호출 지점

단순히 함수 하나가 아니라, **하나의 기능이 엔드포인트에서 의존성·검증·예외까지 이어지는 흐름 전체**를 하나의 테스트 단위로 묶어 인식합니다.

**② 테스트 케이스 매트릭스 생성**

추출한 기능마다 테스트 후보를 정상·실패·경계·보안 관점으로 나누어 자동 생성합니다. 사람이 놓치기 쉬운 예외·경계 케이스까지 함께 제시하는 것이 핵심입니다.

예시 — 로그인 기능:

```
[정상]
□ 올바른 이메일/비밀번호 입력 시 토큰 발급

[실패]
□ 존재하지 않는 이메일
□ 잘못된 비밀번호
□ 탈퇴한 계정
□ 잠긴 계정

[경계 / 검증]
□ 이메일 형식 오류
□ 비밀번호 빈 값
□ 요청 Body 필드 누락

[보안]
□ 반복 로그인 실패
□ 권한 없는 사용자의 보호 API 접근
```

경계·검증 케이스는 Pydantic 스키마의 제약 조건에서, 실패 케이스는 예외 처리 흐름에서 기계적으로 도출되므로 추측이 아닌 **코드 근거 기반**으로 제시됩니다.

**③ 사용자 조합 기반 테스트 생성**

생성된 케이스 후보를 사용자가 체크박스처럼 선택하고 조합합니다. 도구가 일방적으로 전부 생성하는 대신, 개발자가 우선순위를 정해 필요한 케이스만 골라 테스트로 만들 수 있습니다.

```
선택한 테스트 조합
─────────────────────────
기능:   로그인
상황:   잘못된 비밀번호
상태:   계정 잠김 아님
검증:   401 응답 + ErrorCode.INVALID_PASSWORD
```

**④ 테스트 데이터 자동 생성**

선택한 케이스에 필요한 mock 데이터와 의존성 대체(override) 설정을 자동으로 생성합니다. FastAPI의 `dependency_overrides`와 인메모리 DB 패턴을 활용해, 외부 인프라 없이도 실행 가능한 테스트를 만듭니다.

**⑤ 기능 커버리지 지도**

단순 라인 커버리지가 아니라 **기능 단위 커버리지**로 테스트 현황을 시각화합니다. 어떤 기능이 얼마나 검증되고 있고, 어디에 사각지대가 있는지 한눈에 파악할 수 있습니다.

```
로그인          ████████░░  80%
회원가입        ██████░░░░  60%
주문 처리        ███░░░░░░░  30%
결제            ██░░░░░░░░  20%
```

### 기술 접근

- **분석 엔진**: Python 표준 `ast` 모듈 기반 정적 분석으로 코드를 실행하지 않고 구조·제약·의존성을 추출
- **케이스 설계**: 규칙 기반 도출(스키마 제약, 예외 흐름)을 뼈대로 하고, AI 모델로 도메인 맥락에 맞는 케이스를 보강하는 하이브리드 방식
- **테스트 실행 검증**: FastAPI `TestClient`로 생성된 테스트를 즉시 실행해 통과 여부를 확인하고 수정 제안 제공
- **확장성**: 프레임워크 어댑터 구조로 설계하여, 이후 NestJS 등 구조가 명확한 다른 백엔드 프레임워크로 확장 가능

---

## 4. 기대 효과

**테스트 진입 장벽을 낮춥니다.**
"무엇을 테스트해야 할지 모르겠다"는 가장 큰 장벽을, 코드 근거 기반의 케이스 매트릭스로 대신 제시합니다. 개발자는 빈 화면 앞에서 고민하는 대신, 제안된 케이스를 검토하고 선택하는 것에서 출발할 수 있습니다.

**놓치기 쉬운 예외·경계 케이스를 줄입니다.**
정상 흐름만 검증하고 넘어가기 쉬운 예외·경계·보안 케이스를 스키마와 예외 흐름에서 자동으로 도출해 제시함으로써, 커버리지의 사각지대를 줄이고 배포 전 결함을 조기에 발견할 수 있습니다.

**테스트 설계 과정 자체를 문서화합니다.**
기능 추출 결과와 커버리지 지도는 테스트 도구를 넘어 **프로젝트 이해·온보딩 자료**로도 활용됩니다. 새로 합류한 기여자가 "이 프로젝트가 어떤 기능으로 구성되어 있고, 어디가 검증되어 있는지"를 빠르게 파악할 수 있습니다.

**비어 있는 FastAPI 테스트 설계 시장을 겨냥합니다.**
이 컨셉은 이미 다른 생태계(예: Java의 Diffblue)에서 그 가치가 검증되었습니다. TestWeaver는 검증된 컨셉을, 빠르게 성장하지만 아직 이 수준의 도구가 없는 FastAPI 생태계에 가져옵니다. 새로운 미검증 아이디어가 아니라 **입증된 방향 + 비어 있는 시장**이라는 점에서 실현 가능성과 성장 여지를 동시에 갖습니다.

**오픈소스로 확장 가능한 구조를 지향합니다.**
규칙 기반 케이스 도출은 커뮤니티가 규칙을 추가하는 방식으로 확장할 수 있고, 프레임워크 어댑터 구조는 새로운 백엔드 프레임워크 지원을 기여받기 좋은 형태입니다.
