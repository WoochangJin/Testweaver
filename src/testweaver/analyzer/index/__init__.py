"""Pass 1 — 프로젝트 전역 인덱싱.

여기 있는 모듈들은 "무엇이 어디에 있는가"만 기록하고 해석은 하지 않는다.
해석은 인덱스가 완성된 뒤 Pass 2(추출기)가 담당한다.

수집과 해석을 분리하는 이유는 두 가지다.

  · 순서 의존성 제거 — routers/auth.py 를 먼저 읽든 main.py 를 먼저 읽든
    같은 결과가 나와야 한다.
  · 순환 참조 — 모델 상속, 라우터 중첩, 함수 호출은 서로를 참조한다.
    읽으면서 즉시 해석하려 들면 무한 재귀에 빠진다.
"""
