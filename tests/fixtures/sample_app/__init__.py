"""analyzer 검증용 샘플 FastAPI 프로젝트.

실제 프로젝트에서 흔한 구조(라우터 prefix 중첩, 스키마 분리, 서비스 계층,
의존성 주입, 커스텀 예외)를 의도적으로 담고 있다. 각 요소가 어떤 추출
기능을 검증하기 위한 것인지는 파일별 주석에 적어 두었다.

이 앱은 실제로 import 가능한 유효한 FastAPI 앱이어야 한다.
`app.openapi()` 결과를 정적 분석 결과의 정답지로 쓰기 때문이다.
"""
