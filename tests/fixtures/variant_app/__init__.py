"""타입과 형태가 까다로운 경우만 모은 세 번째 샘플 앱.

`sample_app` 은 흔한 구조, `edge_app` 은 까다로운 선언 방식을 담는다.
여기는 **값이 어떤 이름으로 실려 나가는가**와 **모델의 모양**에 집중한다.

여기서도 결함 세 개가 드러났다.

  · 같은 라우터를 두 곳에 마운트          두 번째 마운트의 라우트가 통째로 빠졌다
  · dict/list 본문                        쿼리 파라미터로 잘못 분류했다
  · Field(alias="displayName")            파이썬 이름을 그대로 내보냈다

이 앱도 import 가능한 유효한 FastAPI 앱이라 openapi() 를 정답지로 쓴다.
"""
